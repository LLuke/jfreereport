#
# Defines, whether images are handled by this target.
# Usually, this produces OutOfMemoryExceptions very soon,
# so it is disabled by default.
org.jfree.report.modules.output.table.rtf.EnableImages=false